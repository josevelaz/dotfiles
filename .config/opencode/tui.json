{
  "$schema": "https://opencode.ai/tui.json",
  "plugin": [
    "@tarquinen/opencode-dcp@beta"
    // Local workstation-only plugins — add back in a machine-local override if needed.
  ],
  "theme": "rose-pine"
}
