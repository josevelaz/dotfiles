set -g default-terminal "${TERM}"
set -g prefix C-a

# Undercurl
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

unbind C-b
bind-key C-a send-prefix
#!/usr/bin/env bash

# Main colors for Night Owl theme
set -g mode-style "fg=#7c4dff,bg=#011627"

set -g message-style "fg=#7c4dff,bg=#011627"
set -g message-command-style "fg=#7c4dff,bg=#011627"

set -g pane-border-style "fg=#011627"
set -g pane-active-border-style "fg=#7c4dff"

set -g status "on"
set -g status-justify "left"

set -g status-style "fg=#7c4dff,bg=#011627"

set -g status-left-length "100"
set -g status-right-length "100"

set -g status-left-style NONE
set -g status-right-style NONE

set -g status-interval 2

# Updated colors to match Night Owl theme
set -g status-left "#[fg=#011627,bg=#c792ea,bold] #S #[fg=#c792ea,bg=#011627,nobold,nounderscore,noitalics]"
set -g status-right '#[fg=#c792ea]#($TMUX_PLUGIN_MANAGER_PATH/tmux-mem-cpu-load/tmux-mem-cpu-load --interval 2)#[default]'

# Window status colors adjusted to match Night Owl theme
setw -g window-status-activity-style "underscore,fg=#c792ea,bg=#011627"
setw -g window-status-separator ""
setw -g window-status-style "NONE,fg=#c792ea,bg=#011627"

# Using consistent colors for window status format
setw -g window-status-format "#[fg=#c792ea,bg=#011627,nobold,nounderscore,noitalics] #I  #W #F #[fg=#c792ea,bg=#011627,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=#011627,bg=#7c4dff,nobold,nounderscore,noitalics]#[fg=#7c4dff,bg=#011627,bold] #I  #W #F #[fg=#7c4dff,bg=#011627,nobold,nounderscore,noitalics]"

# tmux-plugins/tmux-prefix-highlight support
set -g @prefix_highlight_output_prefix "#[fg=#ffc777]#[bg=#1e2030]#[fg=#1e2030]#[bg=#ffc777]"
set -g @prefix_highlight_output_suffix ""

unbind %
bind | split-window -h 

unbind '"'
bind - split-window -v

unbind r
bind r source-file ~/.tmux.conf

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind -r m resize-pane -Z

set -g mouse on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"

unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

# remove delay for exiting insert mode with ESC in Neovim
set -sg escape-time 10

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

# list of tmux plugins
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
# set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes
set-option -g @plugin 'b0o/tmux-autoreload'
set -g @plugin 'thewtex/tmux-mem-cpu-load'


#### TMP ####
# set -g @resurrect-capture-pane-contents 'on'
# set -g @continuum-restore 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
